# Smithery configuration for email-playbook-mcp
# Discovery: https://smithery.ai
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    required: []
  commandFunction: |-
    (config) => ({
      command: 'npx',
      args: ['-y', 'email-playbook-mcp@latest'],
      env: {}
    })
