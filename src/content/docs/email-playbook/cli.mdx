---
title: AI Integration (MCP + CLI)
description: Use the Email Playbook directly inside Claude Desktop, Cursor, your terminal, or any HTTP/JSON-RPC client. Three install paths from the same npm package.
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

This playbook is published as an [MCP server](https://modelcontextprotocol.io/) + terminal CLI so AI clients and dev workflows can pull rules and components into whatever they're building. The model (or you) calls structured tools, you stay out of copy-paste hell, and the same rules ship to every surface.

## Install in your client

Pick your client and paste. The config uses `npx` so there's no global install step — `email-playbook-mcp@latest` is pulled on first launch and cached.

<Tabs syncKey="mcp-client">
<TabItem label="Claude Desktop">

Edit `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS) or `%APPDATA%\Claude\claude_desktop_config.json` (Windows):

```json
{
  "mcpServers": {
    "email-playbook": {
      "command": "npx",
      "args": ["-y", "email-playbook-mcp@latest"]
    }
  }
}
```

Restart Claude Desktop. The four tools appear in the model's tool list automatically.

</TabItem>

<TabItem label="Cursor">

Edit `~/.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "email-playbook": {
      "command": "npx",
      "args": ["-y", "email-playbook-mcp@latest"]
    }
  }
}
```

Restart Cursor. Tools show up under the MCP panel.

</TabItem>

<TabItem label="Cline / Continue / Windsurf">

Same shape, different file. Wherever the client stores MCP server config, add:

```json
{
  "mcpServers": {
    "email-playbook": {
      "command": "npx",
      "args": ["-y", "email-playbook-mcp@latest"]
    }
  }
}
```

</TabItem>

<TabItem label="Codex CLI">

```bash
codex mcp add email-playbook -- npx -y email-playbook-mcp@latest
```

</TabItem>

<TabItem label="Hosted (no install)">

Point any HTTP client at `https://docs.osamahassouna.com/api/mcp` for JSON-RPC 2.0. Useful for scripts, remote agents, or browser-based clients.

```bash
curl -X POST https://docs.osamahassouna.com/api/mcp \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'
```

</TabItem>
</Tabs>

<Aside type="tip" title="Three install paths, one package">
**MCP server (local)** — what the tabs above install. Client spawns `email-playbook-mcp` via stdio.

**CLI (local)** — the same npm package also ships an `email-playbook` terminal command for piping and scripting.

**Hosted JSON-RPC** — no install at all. See the Hosted tab.
</Aside>

## What it gives the model

Four tools. All read-only, all deterministic, all serving the same content you read on this site.

| Tool | What it returns |
|---|---|
| `list_categories` | The four rule categories (structure, compatibility, production, ai-generation) with page counts. |
| `get_playbook_rules` | Full rule pages for a category — title, description, body, all code examples. |
| `list_components` | All six components (buttons, spacing, images, inline-icon, background-images, text) with metadata: subcategory, VML flag, responsive flag. |
| `get_component` | Full record for one component — title, description, narrative, HTML pattern, slots, examples. |

The model uses these to discover and quote the exact playbook patterns instead of hallucinating its own table layout from training data. Components live in their own dimension — use `list_components` / `get_component`, not `get_playbook_rules`.

## CLI

The same package ships a terminal command for direct use:

```bash
email-playbook --help

email-playbook list-categories
email-playbook get-rules structure
email-playbook list-components
email-playbook get-component buttons
```

Output is pretty-printed JSON to stdout, so it pipes cleanly:

```bash
email-playbook get-component buttons | jq '.slots'
email-playbook list-components | jq -r '.[] | select(.requires_vml) | .name'
```

Exit codes are scripting-friendly: `0` success, `1` runtime error, `2` usage error.

## Hosted endpoint

Skip the install. Send JSON-RPC 2.0 requests to `https://docs.osamahassouna.com/api/mcp`.

### Discover

```bash
curl https://docs.osamahassouna.com/api/mcp
```

Returns server info and supported methods.

### List tools

```bash
curl -X POST https://docs.osamahassouna.com/api/mcp \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'
```

### Call a tool

```bash
curl -X POST https://docs.osamahassouna.com/api/mcp \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "id": 2,
    "method": "tools/call",
    "params": {
      "name": "get_component",
      "arguments": { "name": "buttons" }
    }
  }'
```

Returns the bulletproof button pattern, its slots, and metadata.

<Aside type="note" title="No auth">
The content is public — the same rules you read here, served as JSON. No tokens needed.
</Aside>

## Example flow inside an AI client

Once installed, ask the model something like:

> "Build me a transactional welcome email with a CTA button. Use the email playbook tools to follow the patterns exactly."

The model will typically:

1. Call `list_categories` to orient itself.
2. Call `get_playbook_rules` for `structure` and `compatibility` to get the boilerplate + responsive pattern.
3. Call `list_components` and `get_component` for the components it needs (button, spacer).
4. Assemble the email using the exact HTML patterns from the playbook.

The output renders correctly in Outlook + Gmail + Apple Mail because it never deviated from the playbook's table layout, VML, and inline-style conventions.

## Source

- npm package: [`email-playbook-mcp`](https://www.npmjs.com/package/email-playbook-mcp)
- GitHub: [`OsamaHassouna/docs-hub`](https://github.com/OsamaHassouna/docs-hub) — `mcp/` directory
- License: MIT
